"""Точка входа для локального запуска приложения.

Относится к заданиям:
- 10.1, 10.2: запуск API с пользовательскими ошибками и валидацией;
- 11.1, 11.2: приложение удобно поднимать перед ручной проверкой и тестами.
"""

import uvicorn

from app.main import app


if __name__ == "__main__":
    # Запускаем приложение через строку "app.main:app", чтобы Uvicorn мог
    # корректно перезагружать сервер при изменении файлов в режиме reload.
    uvicorn.run("app.main:app", host="127.0.0.1", port=8000, reload=True)
